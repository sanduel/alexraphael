---
import type { CollectionEntry } from "astro:content";

interface Props {
  tour: CollectionEntry<"tours">;
}

const { tour } = Astro.props;
const { title, date, duration, meetingPoint, bookingUrl } = tour.data;

const dateLabel = new Intl.DateTimeFormat("en-US", {
  weekday: "short",
  month: "short",
  day: "numeric",
  hour: "numeric",
  minute: "2-digit",
  timeZone: "America/New_York",
}).format(date);
---

<article class="border border-ink/15 rounded-sm p-6 md:p-7 bg-white/40 flex flex-col gap-3">
  <p class="text-xs uppercase tracking-[0.18em] text-ink/60">
    {dateLabel}
  </p>
  <h3 class="text-2xl md:text-3xl leading-tight">{title}</h3>
  <p class="text-sm text-ink/70">
    {duration} · meets at {meetingPoint}
  </p>
  {
    bookingUrl && (
      <a
        href={bookingUrl}
        target="_blank"
        rel="noopener"
        class="mt-4 inline-flex items-center justify-center gap-2 px-5 py-3 rounded-sm bg-accent text-bg text-sm font-medium uppercase tracking-[0.15em] hover:bg-ink transition self-start"
      >
        <span>Book this tour</span>
        <span aria-hidden="true">→</span>
      </a>
    )
  }
</article>
